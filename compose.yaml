services:
    app:
        image: node:24-alpine
        volumes:
            - ./:/var/www/
        working_dir: /var/www/

    prod:
        image: node:24-alpine
        profiles:
            - prod
        volumes:
            - ./:/var/www/
        working_dir: /var/www/
